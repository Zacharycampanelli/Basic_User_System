<div class='card my-3'>
  {{!-- <div class='card-body d-flex flex-column justify-content-center'> --}}
    <div class="mt-0 p-5 bg-primary text-white rounded border-bottom">
    <h2 class='card-title text-center'>Welcome to Your Digital Notebook</h2>

<ul class="list-group">
{{#each posts as |post| }}
<li class="list-group-item d-flex justify-content-between align-items-center">
  {{> post-info post}}
</li>
{{/each}}
</ul>
</div>
    {{!-- {{#if loggedIn}}
      {{! <a href=""></a> }}
      <h2>{{user.username}}</h2>
    {{else}}
      <a href='/signup' class='btn btn-primary text-white mx-auto mt-4'> Get Started Now!</a>
    {{/if}} --}}
    
  </div>
</div>